{
  "name": "claude-browse",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "build": "bun build src/cli.ts --compile --outfile bin/browse"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  }
}
